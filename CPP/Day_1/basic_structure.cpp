//! BASIC STURCTURE OF C++.

#include <iostream>  //? Including Header File called (IOSTREAM) its full form is "Input Output" stream.
using namespace std; //? It use std named "OBJECT" which comes from "header file" and "std" means "STANDARD".

int main() //* It is an entry point of program.It is a main funtion execution process start's from here.
{
    cout << "Hello World!"; //* "COUT" is also an another "OBJECT" in simple terms it is a 'predefined block' which is 'mentioned' in or "written" in header file.

    return 0; //! Its is an exit point of program.Execution process end's here.
}